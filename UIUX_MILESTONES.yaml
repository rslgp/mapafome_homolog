# UIUX_MILESTONES.yaml — roadmap vivo de UI/UX do MAPA FOME.
# Dono: sessao ICT6 / Principal Staff UI/UX (mandato permanente,
# vault 10-DECISOES/2026-07-03-mapafome-mandato-ict6-uiux).
#
# Formato da casa (mesmo idioma do MILESTONES.yaml): cada item carrega
# id, title, tier, owner, scope (Include/Exclude), why, gate, status.
# Regras de operacao (append-only, nunca reescrever historico):
#   - tier: S+ (move o KPI diretamente) > S > A > B. Trabalhar do maior para o menor.
#   - status: pending -> in-progress -> shipped (SO com gate verde LIDO na sessao) | later | blocked-human.
#   - "continue" = pegar o proximo pending de maior tier sem depends abertos.
#   - Todo shipped ganha o campo commit (hash + subject).
#   - human_gate: true = decisao/acao do humano (deploy, endpoint de telemetria,
#     consentimento, politica de produto). Claude PROPOE, nao executa.
#   - Gate minimo de qualquer item que toque src/: lint + test + fitness + build +
#     smoke200 (+ a11y quando UI). Nunca pular smoke200.
#
# Governadores fixos do produto (nao negociaveis por item):
#   - Tom calmo / dignidade: nenhum padrao de pressao (pulse infinito, urgencia falsa,
#     contadores inflados). Ver InfoPanel Art.135 framing: INTENCIONAL, nao re-flagar.
#   - WCAG 2.2 AA e bloqueador de ship (SC 2.2.2 / 2.3.3 / 2.4.7 / 1.4.11 ja pegaram
#     defeitos reais neste roadmap).
#   - Personas de teste: Android low-end, tela ao sol, baixa alfabetizacao digital.
#
# KPI norte (definido na FASE 2; numeros de baseline vem de medicao, nunca de chute):
#   KPI-1: % de sessoes com scroll-depth > 25% (hoje ~20% por teste com usuarios; meta > 60%).
#   KPI-2: % de sessoes que interagem com QUALQUER superficie abaixo do mapa (meta > 40%).
#   KPI-3: taxa de clique nos CTAs de receita (assinar + bluey) por sessao (baseline a medir).

milestones:
  # ---------- FASE 0 — Scroll-discovery pass (SHIPPED 2026-07-03) ----------
  - id: UX-M00
    title: "Pass de scroll-discovery: barras apple-like, pulse limitado nos CTAs de receita, 7 affordances de 'tem mais conteudo'"
    tier: S+
    owner: ict6-uiux-session
    scope: "Include: scrollbars finas translucidas globais (SOT em tokens.css), trilho de pills des-escondido + scroll-snap + veus de borda + chevron de arraste, ScrollAffordance (barra de progresso 3px + veu inferior pre-scroll), mapa como card elevado, 60vh->56vh mobile (peek), ViewMoreCue como botao-pilula real, .mdf-pulse 6 ciclos em assinar + Bluey, scroll-margin-top nos alvos de ancora. Exclude: gesto do mapa em si (FASE 1)."
    why: "Teste com usuarios: ~80% nunca rolam; acham que a pagina e so a primeira tela."
    gate: ["npm run lint", "vitest forks 1334/1334", "npm run fitness", "npm run build", "npm run smoke200", "axe-core CLI / (0 violacoes)"]
    status: shipped
    commit: "3f8bcec feat(ux): surface below-map content and pulse revenue CTA"

  # ---------- FASE 1 — Raiz do problema: o mapa engole o gesto ----------
  - id: UX-M01
    title: "Desktop cooperative-wheel: scrollWheelZoom desligado ate click/focus no mapa (padrao Google-embed)"
    tier: S+
    owner: ict6-uiux-session
    scope: "Include: scrollWheelZoom desligado ate primeiro click/focus dentro do mapa (wheelGate puro + CooperativeWheelZoom via portal); hint pointer-only 'clique para ativar o zoom' (i18n 12 locales, chave page.map.wheel_hint); wheel pre-ativacao da nudge no hint; flag build-time NEXT_PUBLIC_COOP_GESTURES para rollback por rebuild (off = legado byte-identico). Exclude: touch (UX-M02); dependencia nova (implementacao caseira, sem leaflet-gesture-handling)."
    why: "Wheel sobre o mapa hoje da zoom em vez de rolar a pagina — causa numero 1 do scroll-trap em desktop."
    gate: ["npm run test", "npm run lint", "npm run build", "npm run smoke200", "tap-compat e2e verde"]
    status: shipped
    commit: "3e8359e feat(map): gate wheel-zoom behind cooperative engagement"
    evidence: "eslint 0; vitest 1340/1340 (6 testes wheelGate novos + paridade i18n); fitness verde; build 0; smoke200 16/16; axe 0; e2e country-pick Brasil+Franca verdes com o novo contrato (focus antes do wheel); 2 falhas e2e de disclosure provadas PRE-EXISTENTES via stash-rerun (gap CSS [hidden] do .mdf-flag__panel, concern separado)."

  - id: UX-M02
    title: "Mobile two-finger pan: 1 dedo rola a pagina, 2 dedos movem o mapa, com veu educativo"
    tier: S+
    owner: ict6-uiux-session
    scope: "Include: dragging desabilitado para toque unico + veu semitransparente 'use dois dedos para mover o mapa' (aparece so no gesto errado, some em 1.5s, i18n); pinch-zoom e 2-finger pan preservados; NAO pode quebrar o contrato de tap (drop pin por tap, long-press contextmenu — suite tap-compat inteira e o guarda); mesma flag FF_COOP_GESTURES. Exclude: desktop (UX-M01)."
    why: "No touch, arrastar com 1 dedo sobre 56vh de mapa pana o mapa e nunca rola a pagina; e o scroll-trap dos ~80%."
    gate: ["npm run test", "tap-compat e2e verde em touch profile", "npm run build", "npm run smoke200"]
    status: shipped
    depends: [UX-M01]
    commit: "fcc420a feat(map): move map with two fingers, scroll page with one"
    evidence: "eslint 0; vitest 1346/1346 (+6 touchGate); fitness verde; build 0; smoke200 16/16; axe 0; e2e mobile-chrome (interactions+intl-marking) 5 passed — unicas falhas sao as 2 de disclosure ja provadas pre-existentes (gap CSS [hidden], concern separado). Listeners passivos, sem preventDefault: contrato de tap/long-press intacto."

  - id: UX-M03
    title: "Suite e2e de gesture-handling (regressao do contrato de tap + do scroll da pagina)"
    tier: S
    owner: test-playwright-e2e-engineer
    scope: "Include: specs Playwright — wheel sobre mapa rola pagina antes do click e da zoom depois; 1-dedo drag rola pagina com flag on; drop-pin por tap e long-press continuam funcionando; flag off = comportamento legado byte-identico. Exclude: unit tests (ja cobertos)."
    why: "UX-M01/M02 mexem no seam mais sensivel do produto; sem rede de regressao viram roleta."
    gate: ["npm run test:e2e (specs novos verdes)", "npm run lint"]
    status: shipped
    depends: [UX-M01, UX-M02]
    commit: "3bcae55 test(map): add cooperative-gestures e2e regression lane"
    evidence: "tests/e2e/gestures.e2e.js: 2 passed + 2 skips cross-project corretos, exit 0; eslint 0. Armadilhas documentadas no spec: focus() rola ~170px sob o header sticky (preventScroll no baseline), scroll-behavior:smooth exige scrollTo instant, CDP synthesizeScrollGesture inerte neste headless (probe de controle 0px fora do mapa) -> touch testado por contrato (touch-action pan-y, leaflet-touch-drag ausente, leaflet-touch-zoom presente, veil --on em drag sintetico)."

  # ---------- FASE 2 — Medicao: provar que os 80% caem ----------
  - id: UX-M04
    title: "Beacon de scroll-depth privacy-safe (agregado, sem PII, sem consent-wall se anonimo)"
    tier: S+
    owner: ict6-uiux-session
    human_gate: true
    scope: "Include: proposta tecnica + implementacao atras de aprovacao — evento unico por sessao com bucket de scroll max (0/25/50/75/100), sem ID de usuario, sem coordenada, batched no visibilitychange; destino a decidir pelo humano (Sheets append como o resto do app vs endpoint). LGPD: dado anonimo agregado. Exclude: qualquer coisa com PII ou fingerprint."
    why: "Sem medicao o roadmap inteiro e fe; KPI-1/2/3 precisam de baseline e de prova do delta."
    gate: ["npm run test", "npm run lint", "npm run build", "npm run smoke200", "aprovacao humana do destino dos dados"]
    status: blocked-human

  - id: UX-M05
    title: "Eventos de interacao nos affordances (cue, nudge, veil, pulse, progress) no mesmo beacon"
    tier: S
    owner: ict6-uiux-session
    scope: "Include: contadores agregados por sessao — ViewMoreCue click, nudge click, veil visto/dispensado, CTA assinar/bluey click; mesmo canal do UX-M04. Exclude: heatmaps, session-replay (invasivos, fora do tom do produto)."
    why: "Separa 'affordance que converte' de 'decoracao'; alimenta a decisao do UX-M09."
    gate: ["npm run test", "npm run lint", "npm run build", "npm run smoke200"]
    status: blocked-human
    depends: [UX-M04]
    note: "Bloqueado por transitividade: UX-M04 (canal do beacon) e blocked-human."

  - id: UX-M06
    title: "KPI SOT + leitura antes/depois no /relatorio-marketing"
    tier: A
    owner: ict6-uiux-session
    scope: "Include: secao no relatorio-marketing lendo os agregados do beacon (KPI-1/2/3, semana a semana); definicao dos KPIs como SOT neste arquivo. Exclude: dashboards externos."
    why: "Fecha o loop: cada fase seguinte cita numero, nao impressao."
    gate: ["npm run test", "npm run lint", "npm run build", "npm run smoke200"]
    status: blocked-human
    depends: [UX-M04, UX-M05]
    note: "Bloqueado por transitividade: UX-M04/M05 sao blocked-human."

  # ---------- FASE 3 — Funil de receita ----------
  - id: UX-M07
    title: "/assinar above-the-fold: proposta de valor + PIX default + 'menos de 1 minuto'"
    tier: S+
    owner: ict6-uiux-session
    scope: "Include: reordenar o above-fold do /assinar (valor -> valor sugerido -> PIX default -> tempo estimado); rails continuam pix|cartao|boleto (Asaas NAO tem debito — decisao gravada, nao re-adicionar); cartao continua hosted checkout (PCI — nao re-adicionar form inline). Exclude: precos/valores (humano define), backend asaas."
    why: "O pulse leva o olho ate o CTA; a pagina de destino e quem converte ou perde."
    gate: ["npm run test", "npm run lint", "npm run build", "npm run smoke200", "npm run a11y em /assinar"]
    status: shipped
    evidence: "Reordem above-fold: VALOR primeiro (ancora o compromisso), rail depois (Pix ja e default) — reorder JSX puro, contratos radiogroup intactos; selo calmo 'leva menos de 1 minuto' (chave assinar.time_estimate x12). Gate: lint 0, vitest 1346/1346, fitness, build 0, smoke200 16/16, axe 0 em / E em /assinar. Nao tocou rails nem precos (Exclude respeitado)."

  - id: UX-M08
    title: "Prova social digna no /assinar (contador real de apoiadores, nunca inflado)"
    tier: S
    owner: ict6-uiux-session
    human_gate: true
    scope: "Include: contador de assinantes ativos vindo de dado real (fonte a aprovar pelo humano); esconder abaixo de N=10 (numero baixo constrange em vez de provar); copy calma ('voce estaria em boa companhia', nunca 'so hoje!'). Exclude: qualquer contador sintetico ou urgencia fabricada."
    why: "Prova social e o maior alavancador etico de conversao; o teto e a dignidade do produto."
    gate: ["npm run test", "npm run lint", "npm run build", "npm run smoke200", "aprovacao humana da fonte do numero"]
    status: blocked-human

  - id: UX-M09
    title: "Decisao data-driven do pulse: manter 6 ciclos, reduzir ou remover"
    tier: A
    owner: ict6-uiux-session
    scope: "Include: comparar CTR do CTA (UX-M05) em builds com pulse on/off (flag build-time, coortes semanais — o site e estatico, A/B real nao existe sem infra); decidir e documentar aqui. Exclude: aumentar o pulse alem de 6 ciclos (teto WCAG SC 2.2.2 + tom)."
    why: "O pulse e hipotese, nao dogma; se nao mover KPI-3, morre (menos motion = melhor)."
    gate: ["dados de >= 2 semanas por coorte lidos", "npm run build", "npm run smoke200"]
    status: blocked-human
    depends: [UX-M05]
    note: "Bloqueado por transitividade (cadeia M04->M05) + exige 2 semanas de dados reais."

  - id: UX-M10
    title: "/bluey com retorno ao funil (link discreto 'apoie o MAPA FOME')"
    tier: A
    owner: ict6-uiux-session
    scope: "Include: cross-link calmo do /bluey de volta ao /assinar; consistencia visual com o resto (tokens). Exclude: redesign do /bluey."
    why: "Bluey gera trafego de criancas/familias; hoje e beco sem saida no funil."
    gate: ["npm run lint", "npm run build", "npm run smoke200"]
    status: later
    note: "Re-escopado 2026-07-03: /bluey neste repo e so um redirect imediato para o site EXTERNO rslgp.github.io/bluey (outro repositorio). O cross-link pertence aquele repo; uma pagina interstitial local atrasaria a crianca a caminho do jogo (anti-proposito). Humano decide levar o link ao repo externo."

  # ---------- FASE 4 — Compreensao first-run ----------
  - id: UX-M11
    title: "Reativar os 3 passos numerados do StepsHint integrados ao GuidedTutorial"
    tier: S
    owner: ict6-uiux-session
    human_gate: true
    scope: "Include: reavaliar o fluxo guiado hoje desativado (STEPS existe no codigo, render comentado); proposta de reativacao com os seletores ja espelhados do GuidedTutorial; decisao de produto e do humano (foi desativado 'em revisao'). Exclude: reescrever o GuidedTutorial."
    why: "Passos numerados sao o mapa mental do first-run; desativados, o onboarding depende de sorte."
    gate: ["npm run test", "npm run lint", "npm run build", "npm run smoke200", "aprovacao humana de reativar"]
    status: blocked-human

  - id: UX-M12
    title: "Auditar EmptyViewportOverlay: mensagem + CTA quando o viewport tem 0 pontos"
    tier: A
    owner: ict6-uiux-session
    scope: "Include: copy e CTA do overlay de viewport vazio (hoje existe; auditar acao sugerida — 'afaste o zoom' vs 'seja o primeiro a marcar'); i18n 15 locales. Exclude: logica de dados do mapa."
    why: "Primeiro contato num municipio sem pontos e a pior primeira impressao possivel; o overlay e a unica defesa."
    gate: ["npm run test", "npm run lint", "npm run build", "npm run smoke200"]
    status: shipped
    evidence: "AUDITORIA: o EmptyViewportOverlay ja satisfaz o Include — copy digna (empty.no_pins_in_view i18n, 'ninguem foi mapeado ainda, toque em Relatar') + CTA Relatar direto + fechar/swipe, re-arma quando reentra a area vazia. FIX de paridade aplicado: aria-label do region estava hardcoded pt ('Nenhum ponto por perto') -> chave empty.region x12. CTA 'seja o primeiro a marcar' JA e a semantica do Relatar; 'afaste o zoom' descartado (o overlay ja aparece so quando o viewport ESTA vazio; sugerir zoom-out competiria com o CTA nuclear). Gate do lote: lint 0, vitest 1346/1346, fitness, build 0, smoke200 16/16, axe 0."

  - id: UX-M13
    title: "Microcopy one-shot 'o que significam as cores' no primeiro pin visivel"
    tier: A
    owner: ict6-uiux-session
    scope: "Include: tooltip/chip dispensavel (localStorage one-shot) explicando amarelo/azul/vermelho no primeiro render com pins; calmo, dispensavel, i18n. Exclude: repetir a legenda inteira do InfoPanel."
    why: "A cor e o vocabulario central do mapa e hoje so e explicada abaixo do fold — exatamente onde 80% nao chegam."
    gate: ["npm run test", "npm run lint", "npm run build", "npm run smoke200"]
    status: shipped
    evidence: "ColorsHint (one-shot localStorage mdf_colors_hint_seen) renderiza ACIMA do mapa na 1a vez que ha pins; 4 dots (tokens tour-swatch) + linha calma amarelo/azul/vermelho/verde (chave page.map.colors_hint x12) + fechar >=44px; role=note, storage try/catch (private mode nunca trava). VERIFICADO no build servido via Playwright: com 858 markers renderiza (count 1), fechar -> count 0 + flag='1', reload -> count 0 (nao repete). Gate: lint 0, vitest 1346/1346, fitness, build 0, smoke200 16/16, axe 0."

  # ---------- FASE 5 — Conteudo below-fold rende mais ----------
  - id: UX-M14
    title: "ContextBar vira mini-barra sticky ao rolar (contagem + botao Lista sempre a mao)"
    tier: S
    owner: ict6-uiux-session
    scope: "Include: versao condensada da ContextBar que gruda sob o header quando a original sai do viewport (IntersectionObserver); Lista a 1 toque de qualquer ponto da pagina. Exclude: mudar o conteudo da barra."
    why: "Depois que a pessoa rola, o caminho de volta para a acao (Lista/contagem) nao pode se perder."
    gate: ["npm run test", "npm run lint", "npm run build", "npm run smoke200", "npm run a11y"]
    status: shipped
    evidence: "Mini-bar condensada fixa sob o header (IntersectionObserver, so quando a original sai POR CIMA; boundingClientRect.top<0 evita ruido pre-fold); textos aria-hidden (nao duplica live region), botao Lista acessivel; z 885 abaixo do header/progress. Gate do lote: lint 0, vitest 1346/1346, fitness, build 0, smoke200 16/16, axe 0."

  - id: UX-M15
    title: "Hierarquia do InfoPanel por frequencia de uso (sem tocar no framing Art.135)"
    tier: A
    owner: ict6-uiux-session
    scope: "Include: reordenar blocos do InfoPanel (legenda de cores primeiro, badges de instalacao depois, agradecimentos por ultimo); o acknowledgement 'Omissao de Socorro / Art.135' e INTENCIONAL e permanece intocado. Exclude: reescrever copy dos blocos."
    why: "O below-fold recem-descoberto precisa recompensar o scroll na ordem certa."
    gate: ["npm run test", "npm run lint", "npm run build", "npm run smoke200"]
    status: shipped
    evidence: "Legenda de cores (.ip-legend) hoisted para logo apos a secao 'quem a plataforma atende', ANTES de ip-apps/badges e links secundarios — vocabulario nuclear do mapa vira a 1a coisa que o scroller encontra. Reorder JSX puro (bloco recortado 1x e reinserido). O framing Art.135 (acknowledgements) INTOCADO conforme Exclude/memoria infopanel-art135-intentional. Gate do lote: lint 0, vitest 1346/1346, fitness, build 0, smoke200 16/16, axe 0."

  - id: UX-M16
    title: "Footer com mapa-do-site textual (todas as rotas, links reais)"
    tier: B
    owner: ict6-uiux-session
    scope: "Include: bloco de links no fim do main (pets, assinar, imprensa, parceiros, relatorios, iniciativas, privacy, terms); acessivel pre-hidratacao. Exclude: SEO tecnico alem dos links (sitemap.xml ja e outro assunto)."
    why: "Ultimo recurso de descoberta + fim de pagina claro (a pagina 'termina' num lugar util)."
    gate: ["npm run lint", "npm run build", "npm run smoke200"]
    status: shipped
    evidence: "SiteFooterNav (nav aria-label i18n x12 'page.footer.nav_aria') com 9 rotas no fim do main, antes do VersionFooter; links reais pre-hidratacao, alvo >=44px. Labels em pt (nomes proprios das paginas, que sao superficies pt) — decisao registrada. Gate do lote: lint 0, vitest 1346/1346, fitness, build 0, smoke200 16/16, axe 0."

  # ---------- FASE 6 — Internacional e plataforma ----------
  - id: UX-M17
    title: "RTL correctness dos affordances novos (locale ar): propriedades logicas + nudge invertido"
    tier: S
    owner: ict6-uiux-session
    scope: "Include: veus de borda, chevron de arraste e fades do StepsHint com inset-inline/margin-inline (hoje left/right fisicos — em arabe o chevron aponta para o lado errado); useRailEdgeFades ciente de scrollLeft negativo/invertido em RTL; verificar barra de progresso e veu inferior. Exclude: traducao (ar ja existe)."
    why: "O app tem locale arabe; affordance espelhada errada e pior que nenhuma (aponta para o vazio)."
    gate: ["npm run test", "npm run lint", "npm run build", "npm run smoke200 com dir=rtl smoke manual"]
    status: shipped
    evidence: "useRailEdgeFades agora fisico-correto em RTL (Math.abs(scrollLeft) + direction computada; veu esquerdo/direito mapeado por atStart/atEnd); chevron gated por atEnd (direcao de leitura), posicionado com inset-inline-end e glifo espelhado em [dir=rtl]; scrollBy com sinal por direcao; barra de progresso transform-origin right em RTL. engine.js:93 ja seta dir por locale. Gate do lote: lint 0, vitest 1346/1346, fitness, build 0, smoke200 16/16, axe 0."

  - id: UX-M18
    title: "Dark mode dos tokens de affordance (scrollbar thumb, veus, veil inferior)"
    tier: A
    owner: ict6-uiux-session
    scope: "Include: variantes prefers-color-scheme dos tokens novos (thumb ink-claro, veil com surface escura — hoje o rgba 250,250,247 do veil e light-only); NAO e o dark mode do app inteiro. Exclude: dark mode completo do produto (projeto proprio)."
    why: "Nos aparelhos low-end o dark mode do sistema e comum (economia de bateria); veil claro em tema escuro vira mancha."
    gate: ["npm run lint", "npm run build", "npm run smoke200"]
    status: later
    note: "Re-escopado 2026-07-03 (ICT6): o app FORCA tema claro (body surface-0 claro sempre; nao ha dark mode global). Variantes dark so dos tokens de affordance escureceriam scrollbar/veu sobre superficie CLARA — errado. Este item so faz sentido DEPOIS de um dark mode de produto (projeto proprio, Exclude deste item). Nao shipar errado para zerar o backlog."

  - id: UX-M19
    title: "CWV budget: LCP do mapa (preconnect tiles, defer do nao-critico) medido antes/depois"
    tier: A
    owner: vercel:performance-optimizer
    scope: "Include: preconnect/dns-prefetch dos tile servers, auditoria do que bloqueia o first paint, budget documentado (LCP < 2.5s em Moto G-class); medicao Lighthouse CLI gravada aqui. Exclude: trocar provedor de tiles."
    why: "Personas low-end: cada segundo de mapa branco e um usuario que fecha antes de qualquer affordance existir."
    gate: ["lighthouse CLI antes/depois lido", "npm run build", "npm run smoke200"]
    status: pending

  - id: UX-M20
    title: "Skip-link 'pular o mapa' + auditoria de ordem de foco da pagina inteira"
    tier: S
    owner: a11y-architect
    scope: "Include: segundo skip-link (apos o existente) direto para o conteudo pos-mapa; tab-order audit documentado (header -> rail -> mapa -> controles -> cue -> contexto -> painel); armadilhas de foco do Leaflet anotadas. Exclude: refazer o skip-link existente."
    why: "Usuario de teclado atravessa o mapa inteiro (dezenas de tab stops do Leaflet) para chegar ao conteudo — o scroll-trap tem um irmao de teclado."
    gate: ["npm run a11y", "npm run test", "npm run lint", "npm run smoke200"]
    status: shipped
    evidence: "Segundo skip-link 'Pular o mapa e ir ao conteudo' (#MoreInfo, que ja tem scroll-margin-top), chave page.header.skip_past_map x12; auditoria de tab-order documentada em docs/a11y-tab-order.md (ordem completa + armadilhas Leaflet + regra para novos elementos). Gate do lote: axe 0, vitest 1346/1346, lint 0, smoke200 16/16."

  # ---------- FASE 7 — Guardioes (a licao vira forcing function) ----------
  - id: UX-M21
    title: "FF10: proibir animation infinite em CSS fora de allowlist curada"
    tier: A
    owner: softwareengineer
    scope: "Include: fitness function nova no fitness-functions.mjs — grep de 'animation:' com 'infinite' em src/**/*.css fora de FF10_BASELINE (spinner de loading e o unico candidato legitimo hoje); mesma forma do FF9. Exclude: animacoes JS."
    why: "SC 2.2.2 ja pegou defeito real nesta sessao (pulse infinito); regra que so vive em nota de vault repete o erro — gate nao esquece."
    gate: ["npm run fitness (FF10 verde + falha em fixture de teste)", "npm run test"]
    status: shipped
    evidence: "FF10 no fitness-functions.mjs: varre src/**/*.css (comentarios stripped preservando linha), falha em 'animation ... infinite' / 'animation-iteration-count: infinite' fora do FF10_INFINITE_ALLOWLIST (pets.css skeleton + ReportSheet.css spinner — loading indicators, a excecao canonica, com razao). PROVADO: fixture src/app/_ff10_fixture.css fez o gate falhar (exit 1, mensagem FF10 com file:line) e o tree limpo voltou verde (exit 0)."

  - id: UX-M22
    title: "Smoke visual Playwright dos affordances (veil some ao rolar, progress cresce, fades reagem)"
    tier: B
    owner: test-playwright-e2e-engineer
    scope: "Include: 3 asserts e2e — .mdf-scrollveil opacity 0 apos scroll; scaleX da progress bar cresce com scroll; mdf-steps--fade-right aparece quando o rail transborda. Exclude: screenshot-diff pixel-perfect (flaky no HDD lento — licao playwright-flake conhecida)."
    why: "Affordances sao JS+CSS acoplados a scroll real; unit test nao prova que aparecem."
    gate: ["npm run test:e2e (specs novos verdes)", "npm run lint"]
    status: shipped
    depends: [UX-M00]
    commit: "(lote — tests/e2e/affordances.e2e.js)"
    evidence: "tests/e2e/affordances.e2e.js: veil inferior presente em repouso e some apos scroll (--off); barra de progresso cresce (scaleX via DOMMatrix top<bottom); fade-right do trilho aparece quando transborda e limpa no fim do rail. 5 passed + 1 skip (rail nao transborda em desktop largo — correto). Cada assert skipa de proposito se a affordance nao existe naquele viewport (nao falso-falha). lint 0."

  # ---------- FASE 8 — Formularios e estados de sistema ----------
  - id: UX-M23
    title: "ReportSheet form UX: inputmode/autocomplete corretos, validacao inline calma, erro junto ao campo"
    tier: S
    owner: ict6-uiux-session
    scope: "Include: inputmode=tel/numeric onde cabe, autocomplete semantico, validacao no blur (nunca a cada tecla), mensagem de erro adjacente ao campo com aria-describedby, foco vai ao primeiro erro no submit; preservar o contrato do payload (country/PET_COLORS invariantes existentes). Exclude: mudar campos ou fluxo de publicacao."
    why: "O formulario de marcar um ponto E o produto; cada friccao aqui e uma doacao que nao acontece."
    gate: ["npm run test", "npm run lint", "npm run build", "npm run smoke200", "npm run a11y"]
    status: shipped
    evidence: "ReportSheet ja tinha o esqueleto do Include: status idle/publishing/success/error, errorKey i18n (nao string resolvida, segue troca de locale), inline-error com role=alert ADJACENTE aos campos, clear-on-change, campos disabled durante publish. Adicionado: autoComplete='off' + enterKeyHint nos inputs de detalhe (next) e contato (done) — ambos descrevem dados de TERCEIRO, entao o autofill do proprio usuario nunca deve entrar; enterKeyHint move a tecla de acao do teclado on-screen. Contrato do payload (country/idempotency) intocado. Gate do lote: lint 0, vitest 1354/1354, fitness, build 0, smoke200 16/16, axe 0."

  - id: UX-M24
    title: "Skeleton states: mapa e lista com esqueleto em vez de texto 'loading'"
    tier: S
    owner: ict6-uiux-session
    scope: "Include: skeleton shimmer (respeitando reduced-motion: estatico) para o card do mapa e linhas da ListView enquanto dataMaps carrega; tokens de superficie existentes. Exclude: mudar o fetch/fluxo de dados."
    why: "Percepcao de velocidade em aparelho low-end vale tanto quanto velocidade; texto 'loading' parece quebrado."
    gate: ["npm run test", "npm run lint", "npm run build", "npm run smoke200"]
    status: later
    note: "Re-escopado 2026-07-03 (ICT6): nao ha estado de loading REAL para alimentar um skeleton hoje. O mapa ja mostra CircularProgress (MainControls) durante o fetch; a ListView renderiza sobre dataMaps JA resolvido (sem loading proprio). Um skeleton shimmer seria decoracao sem estado que o dispare — pior que o indicador honesto atual. So faz sentido se/quando a ListView ganhar carregamento assincrono proprio (ex.: paginacao). Nao shipar decoracao para zerar o backlog."

  - id: UX-M25
    title: "UX da fila offline: badge visivel 'N marcacoes aguardando conexao' + confirmacao ao sincronizar"
    tier: A
    owner: ict6-uiux-session
    scope: "Include: superficie de estado para a publishQueue existente (badge no header/ContextBar + toast calmo ao flush com sucesso); nunca perder silenciosamente; cuidado com o poison-pin conhecido (achado no vault). Exclude: mexer na logica da fila."
    why: "A fila offline ja funciona por baixo; invisivel, o usuario acha que a marcacao sumiu e re-publica ou desiste."
    gate: ["npm run test (publishQueue.test.js verde)", "npm run lint", "npm run build", "npm run smoke200"]
    status: shipped
    evidence: "AUDITORIA: o Include ja e atendido pelo OfflineToast + bindOnlineFlush (App.js:849-863) — no mount, queueSize()>0 mostra 'N ponto(s) aguardando conexao'; no flush, onResult mostra 'N publicado(s) agora' (sucesso) ou 'ainda nao foi possivel enviar... tentaremos quando voltar a conexao' (falha, sem perder silenciosamente). Respeita o poison-pin (flush classifica permanente vs transitorio, nao congela a fila — comentario publishQueue.js:9-23). Debito conhecido registrado (NAO shipado como novo): a copy do toast e hardcoded pt (mesma classe do empty.region agora corrigido) — i18n do OfflineToast e um item de traducao separado, nao muda o comportamento que o Include pede. Gate do lote: publishQueue.test.js verde no full run 1346/1346, lint 0, build 0, smoke200 16/16."

  - id: UX-M26
    title: "Estados de erro humanos: falha de fetch do Sheets com mensagem + retry, error boundary com recuperacao"
    tier: A
    owner: ict6-uiux-session
    scope: "Include: mensagem pt-BR digna quando a planilha nao responde ('nao conseguimos carregar os pontos agora' + botao tentar de novo), error boundary React que preserva o header/nav; i18n. Exclude: retry automatico agressivo (custo de rede em plano limitado)."
    why: "Hoje falha de rede degrada em silencio ou em tela crua; publico com dados moveis limitados ve isso com frequencia."
    gate: ["npm run test", "npm run lint", "npm run build", "npm run smoke200"]
    status: shipped
    evidence: "ACHADO real: o IIFE de runMain (appMainBootstrap) nao tinha try/catch — falha de auth/loadInfo/getRows rejeitava em silencio e isLoading ficava true PARA SEMPRE (mapa em limbo de loading, sem saida). Corrigido: try/catch envolve o load, no catch limpa envVariables.rows (retry refaz fetch) + setState loadError. LoadErrorBanner (role=alert, tom calmo risk-low, chaves page.load_error.text/retry x12) renderiza acima do mapa; Tentar de novo -> handleRetryLoad re-roda o bootstrap. handleRetryLoad como class field (render enxuto). Gate do lote: lint 0, vitest 1354/1354, fitness, build 0, smoke200 16/16, axe 0. NB: App.js cruzou 1000 LOC com essa adicao -> decomposto por SOLID/SRP (appLifecycle) pelo principalengineer, NAO baselinado."

  - id: UX-M27
    title: "404/_not-found util: link de volta ao mapa + pets + assinar"
    tier: B
    owner: ict6-uiux-session
    scope: "Include: pagina not-found com navegacao real (o smoke200 ja a exclui como error shell intencional — manter exclusao). Exclude: redirects automaticos."
    why: "Link quebrado compartilhado em grupo de WhatsApp nao pode ser beco sem saida."
    gate: ["npm run lint", "npm run build", "npm run smoke200"]
    status: shipped
    evidence: "src/app/not-found.js (server component, zero JS cliente): mensagem digna + 3 links de volta (mapa, /pets, /assinar). smoke200 mantem a exclusao intencional do error shell. Gate do lote: lint 0, build 0, smoke200 16/16."

  # ---------- FASE 9 — Linguagem visual, tipografia e motion ----------
  - id: UX-M28
    title: "Auditoria tipografica para sol e telas pequenas (caption 13px, line-length, hierarquia do InfoPanel)"
    tier: S
    owner: ict6-uiux-session
    scope: "Include: revisar --mdf-fs-caption 13px nos rotulos criticos (rail, ContextBar) contra a persona tela-ao-sol; max-width de linha no InfoPanel (65-75ch); pesos e escala consistentes via tokens; qualquer texto sobre o mapa ganha fundo/contraste reforcado. Exclude: trocar a font stack."
    why: "Tipografia e o affordance mais barato que existe; 13px cinza sob sol simplesmente desaparece."
    gate: ["npm run a11y", "npm run lint", "npm run build", "npm run smoke200"]
    status: shipped
    evidence: "Rotulos do trilho de atalhos (.mdf-steps__tour e .mdf-steps__pets) subiram de fs-caption 13px para fs-small 14px (legibilidade ao sol; caption fica reservado a metadado secundario). Medida de linha 70ch em todo <p> do InfoPanel (.ip-panel p). Tokens ja fixam o piso 16px de input (globals) e o contraste vive no FF5/axe. Gate do lote: lint 0, vitest 1346/1346, fitness, build 0, smoke200 16/16, axe 0."

  - id: UX-M29
    title: "Iconografia consistente: CTAs e sinais criticos de emoji para SVG tokenizado"
    tier: A
    owner: ict6-uiux-session
    scope: "Include: inventario emoji vs SVG (emoji rende diferente por OS/versao — o coracao do doar ja e png por isso); migrar sinais CRITICOS (patinha, chevrons, estados) para SVG inline com fill em token; emojis decorativos podem ficar. Exclude: redesenhar a marca/pins do mapa (SOT proprio com constantes JS sincronizadas)."
    why: "Em Android antigo o emoji quebra o tom visual e a legibilidade que os tokens garantem em todo o resto."
    gate: ["npm run lint", "npm run fitness (FF9 hex SOT)", "npm run build", "npm run smoke200"]
    status: later
    note: "AUDITORIA (2026-07-03): nao ha sinal CRITICO de ESTADO codificado so por emoji. Levantamento dos glifos: os sinais de estado/direcao (chevrons ›⌄, share ↗, fechar ✕/×) JA sao glifos Unicode MONOCROMATICOS estaveis, nao emoji colorido quebravel; os emojis coloridos (🐾🎮👦👧📺) sao TODOS aria-hidden e pareados com rotulo de TEXTO que carrega o sentido (a legibilidade nao depende deles). Migrar 🐾/📺 para SVG seria estetico, nao de legibilidade critica — over-engineering sem defeito real. Deferido: so vale se um sinal de estado passar a depender SO de emoji (ex.: badge sem texto). Nao shipar migracao cosmetica para zerar o backlog."

  - id: UX-M30
    title: "Motion system: catalogo unico de transicoes (sheets, toasts, overlays) sobre os tokens dur/ease"
    tier: A
    owner: ict6-uiux-session
    scope: "Include: inventariar toda animacao/transicao viva (sheets, toasts, veus, pulse), normalizar sobre --mdf-dur-*/--mdf-ease-* + reduced-motion, documentar o catalogo num comment-block do tokens.css; alimenta o FF10 (UX-M21). Exclude: animacoes novas."
    why: "Motion coerente = produto que parece um so; hoje cada superficie anima do seu jeito."
    gate: ["npm run lint", "npm run build", "npm run smoke200"]
    status: shipped
    depends: [UX-M21]
    evidence: "Catalogo unico documentado no comment-block do tokens.css (SOT): escada de duracao (fast=micro-feedback, base=enter/exit, slow=superficies/ambient), dois easings (out=padrao, out-back=overshoot reservado), regra de loops limitados (pulse+cue 6 ciclos; FF10 barra infinite). AUDITORIA: 48 sites animation/transition em 12 ux/*.css, todos nos tokens; 1 drift real corrigido (.mdf-dropped-pin: 240ms+bezier inline -> var(--mdf-dur-base) var(--mdf-ease-out-back)); o spinner mdf-spin 720ms linear infinite fica (loading, allowlist FF10, linear correto p/ rotacao). Todos zeram sob prefers-reduced-motion via os tokens. Gate: lint 0, build 0, smoke200 16/16."

  # ---------- FASE 10 — Arquitetura de informacao e ergonomia ----------
  - id: UX-M31
    title: "Navegacao consistente entre rotas: voltar ao mapa em 1 toque de /pets, /assinar, /imprensa, /parceiros"
    tier: S
    owner: ict6-uiux-session
    scope: "Include: header minimo compartilhado (marca -> /) nas rotas satelites; hoje /pets e /assinar abrem em nova aba e viram ilhas; auditar target=_blank (manter so onde preserva o estado do mapa de proposito). Exclude: SPA-navigation refactor."
    why: "Cada rota satelite sem caminho de volta vaza usuarios que o pulse e o funil trouxeram."
    gate: ["npm run test", "npm run lint", "npm run build", "npm run smoke200"]
    status: shipped
    evidence: "AUDITORIA (2026-07-03) — o produto ja satisfaz o Include: TODAS as rotas satelites tem volta em 1 toque via Link href='/': /assinar (page.js:127 'assinar.back'), /pets (PetsApp.js:89 'pets.back'), /imprensa (page.js:45), /parceiros (page.js:96), /relatorios (page.js:93), /iniciativas/cadastrar (page.js:82). target=_blank auditado: StepsHint abre pets/solone/bluey em nova aba DE PROPOSITO (preserva o estado do mapa em uso — decisao mantida); demais navegacoes sao same-tab. SiteFooterNav (UX-M16) reforca com indice completo. Zero mudanca de codigo necessaria."

  - id: UX-M32
    title: "Auditoria thumb-zone mobile: acoes primarias alcancaveis com uma mao"
    tier: S
    owner: ict6-uiux-session
    scope: "Include: mapa de calor manual dos alvos (Relatar no topo = zona ruim; Confirmar ponto, Lista, cue = zona boa?), mover/duplicar acoes primarias para o terco inferior quando barato (ex.: FAB Relatar), documentar a decisao aqui. Exclude: redesign do header."
    why: "Persona segura o telefone com uma mao no ponto de onibus; CTA fora do polegar e CTA que nao existe."
    gate: ["npm run test", "npm run lint", "npm run build", "npm run smoke200", "npm run a11y"]
    status: shipped
    evidence: "AUDITORIA thumb-zone mobile (2026-07-03), zonas de Hoober (facil=terco inferior, esticar=meio, dificil=topo): DIFICIL: Relatar/assinar (header topo) — mitigado: a acao central real do fluxo NAO e o header, e tocar o MAPA (zona facil/meio, 56vh) e o gesto primario; assinar tem pulse p/ descoberta, nao urgencia motora. ESTICAR: trilho de pills, ContextBar. FACIL: mapa (drop pin = acao nuclear), Confirmar ponto no form, ViewMoreCue, mini-bar Lista (UX-M14 poe Lista de volta ao alcance apos rolar), sheets sobem de baixo (alvos na zona facil). DECISAO: NAO duplicar Relatar como FAB agora — custo visual/atencao permanente contra tom calmo, sem telemetria (UX-M04 blocked-human) para provar demanda; re-avaliar com dados de M05. Nenhuma mudanca de codigo; auditoria e a entrega do Include."

  - id: UX-M33
    title: "Densidade desktop >1024px: aproveitar o espaco (lista lado a lado do mapa)"
    tier: B
    owner: ict6-uiux-session
    scope: "Include: em desktop largo, ListView como painel lateral persistente opcional em vez de sheet modal; grid ja e 2-col, avaliar 3-col. Exclude: mudar mobile."
    why: "Desktop e minoria mas e onde imprensa/parceiros/gestores avaliam o projeto — a primeira impressao institucional."
    gate: ["npm run test", "npm run lint", "npm run build", "npm run smoke200"]
    status: later
    note: "Re-escopado 2026-07-03 (ICT6): transformar a ListView de sheet modal em painel lateral persistente e um refactor grande (contrato do modal/focus-trap, o mesmo seam sensivel dos sheets) para uma minoria de trafego (desktop), sem telemetria (UX-M04 blocked-human) que prove o retorno. O grid mapa+controles ja e responsivo 2-col e a medida de linha 70ch (UX-M28) ja controla a legibilidade em telas largas. Deferido ate haver dado de uso desktop ou pedido institucional explicito; nao vale o risco no seam de modal agora."

  # ---------- FASE 11 — PWA, compartilhamento e pesquisa continua ----------
  - id: UX-M34
    title: "InstallToast com timing de valor: oferecer instalacao apos acao de valor, nao na chegada"
    tier: A
    owner: ict6-uiux-session
    scope: "Include: disparar o InstallToast existente apos 2a visita OU apos primeira acao de valor (publicou ponto, abriu lista), nunca no load inicial; frequencia com cooldown em localStorage. Exclude: mudar o fluxo beforeinstallprompt em si."
    why: "Prompt de instalacao na chegada e ruido que treina o dismiss; depois do valor entregue, e servico."
    gate: ["npm run test", "npm run lint", "npm run build", "npm run smoke200"]
    status: shipped
    evidence: "InstallToast agora exige sinal GANHO antes de convidar (native E iOS branches): 2a+ visita (mdf_visit_count, bumpado 1x/load via ref idempotente compartilhado) OU agiu num pin (mdf_acted_on_pin). Evento beforeinstallprompt ainda capturado; so o REVEAL e segurado. private mode -> nao convida (nunca amola). 3 testes novos (nao-mostra 1a visita, mostra 2a visita, mostra apos acao no pin) + os 5 existentes verdes (semeados com o sinal). Gate do lote: lint 0, vitest 1349/1349, fitness, build 0, smoke200 16/16."

  - id: UX-M35
    title: "Compartilhamento nativo: navigator.share (share sheet + fallback copiar-link)"
    tier: A
    owner: ict6-uiux-session
    scope: "Include: util shareLink puro (navigator.share -> share sheet nativo; fallback clipboard; resultado discriminado shared/copied/cancelled/unavailable) + botao Compartilhar no PinDetailSheet que deep-linka /?lat=&lng= no ponto. Exclude: og-image por rota (movido para UX-M35b — server metadata, risco de export); ListView (mesmo util, adicao trivial depois); SDKs de rede social."
    why: "O canal de crescimento real do publico e WhatsApp; compartilhar um ponto com um toque leva o destinatario ao mesmo lugar no mapa."
    gate: ["npm run test", "npm run lint", "npm run build", "npm run smoke200"]
    status: shipped
    evidence: "shareLink.js (puro, 5 testes: shared, cancel-nao-copia, fallback-clipboard, fallback-em-erro-nao-cancel, unavailable) + botao no PinDetailSheet (deep-link ao ponto, nota 'link copiado' no fallback, track pin_shared). Gate do lote: lint 0, vitest 1349/1349 (+5 shareLink), fitness, build 0, smoke200 16/16, axe 0."

  - id: UX-M35b
    title: "og:title/og:image por rota para preview rico no WhatsApp"
    tier: A
    owner: ict6-uiux-session
    scope: "Include: metadata (Next generateMetadata/export static) com og:title + og:image decente por rota (/, /pets, /assinar, /imprensa) para o card de preview quando o link e colado no WhatsApp; imagem estatica servida do /public. Exclude: og dinamico por ponto (precisa de runtime; o site e output:export estatico)."
    why: "O share (UX-M35) leva o link; um preview bonito e o que faz o grupo clicar. Separado por tocar no build de metadata (export)."
    gate: ["npm run build", "npm run smoke200", "validar og tags no HTML gerado"]
    status: pending

  - id: UX-M36
    title: "Protocolo permanente de teste de usabilidade barato (5 tarefas x 5 usuarios, guerrilla)"
    tier: S
    owner: ict6-uiux-session
    human_gate: true
    scope: "Include: roteiro versionado no repo (docs/usability-protocol.md) — 5 tarefas canonicas (achar comida perto, marcar um ponto, entender as cores, achar o assinar, instalar), criterio de sucesso por tarefa, template de anotacao; humano recruta e conduz; resultados alimentam este arquivo como evidencia de re-priorizacao. Exclude: ferramentas pagas de user-testing."
    why: "Foi um teste com usuarios que revelou os 80%; institucionalizar o teste e o que impede o roadmap de virar opiniao."
    gate: ["documento revisado pelo humano", "npm run lint"]
    status: blocked-human

  # ---------- FASE 6 — Governanca do goal-loop autonomo (ampliacao 2026-07-04) ----------
  # Fonte: auditoria do loop-flow-architect (@loop) sobre agent_goal_loop.md.
  # O elo mais fraco e o Judge (maker==checker) e o budget-guard sem cap/kill/spend-log
  # proprios; estes itens fecham as lacunas antes de qualquer tick nao-supervisionado.
  - id: UX-M37
    title: "Judge distinto no goal-loop: um checker report-only grada o item antes do flip para shipped (maker != checker)"
    tier: S+
    owner: ict6-uiux-session
    scope: "Include: separar o papel de quem constroi de quem aprova no agent_goal_loop — um verifier report-only (reusar loops/agents/verifier.md ou um reviewer dedicado) le o gate verde ANTES de o item virar shipped; o maker nunca assina o proprio trabalho. Exclude: autonomia nao-supervisionada (continua human-gated ate M38 fechar)."
    why: "loop-flow-architect (@loop): LBR-C — o maker nao pode auto-aprovar; com a semana a 85% um auto-grade otimista embarca regressao que nao ha orcamento para re-consertar."
    gate: ["revisao do prompt pelo prompt-engineer", "1 tick de exemplo com o checker no caminho, log lido"]
    status: pending

  - id: UX-M38
    title: "Budget-guard completo do goal-loop: cap por-run + kill-switch (arquivo STOP) + spend-log proprio"
    tier: S+
    owner: ict6-uiux-session
    scope: "Include: o goal-loop passa a ler um guard proprio (cap de tokens por tick, checagem de um arquivo STOP como kill-switch, e uma linha de spend-log por tick) alem do stop suave por % do snapshot; apontar para loops/guard.yaml + loops/STOP se existirem, senao criar o minimo. Exclude: telemetria remota (human_gate)."
    why: "loop-flow-architect (@loop): budget-guard incompleto — hoje so ha stop suave por %, sem cap/kill/spend-log ligados ao proprio goal-loop; pre-requisito de qualquer tick nao-supervisionado."
    gate: ["npm run lint", "1 tick que atinge o cap e para, log lido"]
    status: pending

  - id: UX-M39
    title: "Isolation do goal-loop: cada tick de build roda em worktree/branch proprio, nunca sujando loop/mapafome"
    tier: S
    owner: ict6-uiux-session
    scope: "Include: quando um tick faz build/edita src/, roda num worktree/branch efemero (o padrao ja existe no repo — 8+ worktrees irmaos) e so promove com gate verde; um build vermelho nunca suja a branch viva. Exclude: merge automatico (continua via agent_git-commit-specialist + humano)."
    why: "loop-flow-architect (@loop): Isolation ausente — o loop roda direto na branch viva; um build vermelho num tick deixa a arvore suja para a proxima sessao."
    gate: ["npm run build", "npm run smoke200", "worktree efemero criado e limpo verificado"]
    status: pending

  - id: UX-M40
    title: "Observability do goal-loop: cada tick anexa id + resultado do gate + os 3 numeros de budget lidos a um runlog proprio"
    tier: A
    owner: ict6-uiux-session
    scope: "Include: cada tick do goal-loop anexa uma linha (append-only) a um runlog proprio: id do item, resultado do gate, e context/session/week % lidos naquele tick; torna os ticks auditaveis. Exclude: dashboard visual (fora de escopo do loop)."
    why: "loop-flow-architect (@loop): Observability parcial — loops/runlog.jsonl registra o motor ANTIGO (parado no tick 21); o goal-loop nao anexa nada, entao os ticks sao inauditaveis."
    gate: ["npm run lint", "runlog com >=1 linha valida lida"]
    status: pending
